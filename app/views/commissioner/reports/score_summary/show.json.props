json.period @period || "season"
json.breakdowns @breakdowns
json.from @from
json.to @to
json.teams @report
json.labels do
  @config::STAT_LABELS.each do |key, label|
    json.set! key, label
  end
end
